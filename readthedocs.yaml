version: 2
# 当使用 gh-pages 分支时, 此文件为构建脚本

build:
  commands:
    # 需要在 Read the Docs 网页端把默认分支改为 gh-pages
    - cp -r . "$READTHEDOCS_OUTPUT/html"
    - echo "User-agent: *\nDisallow: /" > "$READTHEDOCS_OUTPUT/html/robots.txt"
